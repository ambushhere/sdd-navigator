---
all:
  hosts:
    k8s-master:
      ansible_host: "{{ k8s_master_ip }}"
      ansible_user: deploy
      ansible_ssh_private_key_file: "~/.ssh/id_ed25519"
  children:
    k8s_controllers:
      hosts:
        k8s-master: {}
